apiVersion: v1
kind: Service
metadata:
  name: flask-db
spec:
  type: ClusterIP
  selector:
    app: flask-db
  ports:
    - port: {{ .Values.postgres.ports.container }}
      targetPort: {{ .Values.postgres.ports.container }}